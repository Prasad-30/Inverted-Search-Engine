# Inverted-Search-Engine
A C-based Inverted Search Engine that indexes multiple text files using a hash table and linked lists to enable fast word-based search, frequency tracking, and efficient data retrieval.
